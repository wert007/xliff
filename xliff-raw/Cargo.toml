[package]
name = "xliff-raw"
version = "0.1.0"
edition = "2024"

[dependencies]
quick-xml = { features = ["serde", "serialize"], git = "https://github.com/wert007/quick-xml.git", version = "0.41.0" }
serde = { version = "1.0.228", features = ["derive"] }
