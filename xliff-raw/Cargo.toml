[package]
name = "xliff-raw"
version = "0.1.0"
edition = "2024"

[dependencies]
quick-xml = { version = "0.39.2", features = ["serde", "serialize"] }
serde = { version = "1.0.228", features = ["derive"] }
